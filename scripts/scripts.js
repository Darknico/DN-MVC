var js_dnmvc = function (params) {

    /* PRIVATE  */
    var self = this;

    /* PUBLIC*/
    var module = {}

    module.init = function () { 
        if (typeof jQuery != 'undefined') {
            // init code here
        }
    }     

    module.init();
    return module;
}